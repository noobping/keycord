name: Windows

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

concurrency:
  group: windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    env:
      FIDO2_LIB_DIR: C:/tools/msys64/mingw64/lib
      PKG_CONFIG_ALLOW_CROSS: 1
      PKG_CONFIG: C:\tools\msys64\mingw64\bin\pkgconf.exe
      MSYS2_PATH: C:\tools\msys64
      PKG_CONFIG_PATH: C:\tools\msys64\mingw64\lib\nettle3\pkgconfig;C:\tools\msys64\mingw64\lib\pkgconfig
      PKG_CONFIG_PATH_x86_64_pc_windows_gnu: C:\tools\msys64\mingw64\lib\nettle3\pkgconfig;C:\tools\msys64\mingw64\lib\pkgconfig
      CARGO_TARGET_X86_64_PC_WINDOWS_GNU_RUSTFLAGS: -Lnative=C:/tools/msys64/mingw64/lib/nettle3
      GETTEXT_SYSTEM: 1

    steps:
      - uses: actions/checkout@v4

      - name: Install MSYS2
        run: choco install msys2 --no-progress -y

      - name: Update MSYS2
        run: C:\tools\msys64\usr\bin\bash -lc "pacman -Syu --noconfirm"

      - name: Install build dependencies
        run: |
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-toolchain"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-capnproto"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-gtk4"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-libadwaita"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-libcbor"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-libfido2"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-gpgme"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-hidapi"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-nettle3"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-pkgconf"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-openssl"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-make"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-ntldd"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-gettext-tools"
          C:\tools\msys64\usr\bin\bash -lc "pacman -S --noconfirm mingw-w64-x86_64-zlib"

      - name: Add MSYS2 paths
        run: |
          echo "C:\tools\msys64\mingw64\bin" >> $env:GITHUB_PATH
          echo "C:\tools\msys64\usr\bin" >> $env:GITHUB_PATH

      - name: Normalize zlib import library name
        shell: pwsh
        run: |
          $mingwLib = 'C:\tools\msys64\mingw64\lib'
          $zImport = Join-Path $mingwLib 'libz.dll.a'
          $zCompat = Join-Path $mingwLib 'libzlib1.dll.a'
          if (-not (Test-Path $zImport)) {
            throw "zlib import library not found: $zImport"
          }
          if (-not (Test-Path $zCompat)) {
            Copy-Item $zImport $zCompat
          }

      - name: Verify native dependency discovery
        shell: pwsh
        run: |
          if (-not (Test-Path $env:FIDO2_LIB_DIR)) {
            throw "FIDO2 lib dir not found: $env:FIDO2_LIB_DIR"
          }
          Get-ChildItem $env:FIDO2_LIB_DIR -Filter 'libfido2*'
          Get-ChildItem $env:FIDO2_LIB_DIR -Filter 'libz*'
          if (-not (Test-Path $env:PKG_CONFIG)) {
            throw "pkg-config helper not found: $env:PKG_CONFIG"
          }
          & $env:PKG_CONFIG --modversion libfido2
          & $env:PKG_CONFIG --libs libfido2

          $nettleHeader = 'C:\tools\msys64\mingw64\include\nettle3\nettle\pgp.h'
          if (-not (Test-Path $nettleHeader)) {
            throw "Nettle 3 compatibility header not found: $nettleHeader"
          }
          $nettleVersion = (& $env:PKG_CONFIG --modversion nettle).Trim()
          if (-not $nettleVersion.StartsWith('3.')) {
            throw "pkg-config selected incompatible Nettle $nettleVersion instead of Nettle 3"
          }
          & $env:PKG_CONFIG --cflags nettle hogweed
          & $env:PKG_CONFIG --libs nettle hogweed

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add GNU Windows target
        run: rustup target add x86_64-pc-windows-gnu

      - name: Build
        run: cargo build --package keycord --features fidopin,docs --locked --release --target x86_64-pc-windows-gnu

      - name: Bundle
        shell: pwsh
        run: |
          $workspace = $env:GITHUB_WORKSPACE
          Write-Host "Workspace: $workspace"

          $bundleDir = Join-Path $workspace 'bundle'
          if (Test-Path $bundleDir) {
            Remove-Item $bundleDir -Recurse -Force
          }
          New-Item -ItemType Directory -Path $bundleDir | Out-Null

          $exe = Join-Path $workspace 'target\x86_64-pc-windows-gnu\release\keycord.exe'
          if (-not (Test-Path $exe)) {
            Write-Error "EXE not found at: $exe"
          }
          Write-Host "Copying exe: $exe"
          Copy-Item $exe $bundleDir -Force

          $mingwBin = 'C:\tools\msys64\mingw64\bin'
          if (-not (Test-Path $mingwBin)) {
            Write-Error "MinGW bin dir not found: $mingwBin"
          }
          Write-Host "Copying DLLs from: $mingwBin"
          Get-ChildItem -Path $mingwBin -Filter '*.dll' |
            Copy-Item -Destination $bundleDir -Force

          $mingwRoot = 'C:\tools\msys64\mingw64'
          $shareDir = Join-Path $bundleDir 'share'

          $iconSourceDir = Join-Path $workspace 'crates\keycord-lifecycle\data\branding\scalable\apps'
          $iconTargetDir = Join-Path $shareDir 'icons\hicolor\scalable\apps'
          if (Test-Path $iconSourceDir) {
            New-Item -ItemType Directory -Path $iconTargetDir -Force | Out-Null
            Copy-Item (Join-Path $iconSourceDir '*.svg') $iconTargetDir -Force
          }

          $iconTargetDir = Join-Path $shareDir 'icons\hicolor\symbolic\apps'
          New-Item -ItemType Directory -Path $iconTargetDir -Force | Out-Null
          $iconSourceDirs = @(
            (Join-Path $workspace 'crates\keycord-shell\data\symbolic\apps'),
            (Join-Path $workspace 'crates\keycord-lifecycle\data\branding\symbolic\apps'),
            (Join-Path $workspace 'crates\keycord-git\data\symbolic\apps')
          )
          foreach ($iconSourceDir in $iconSourceDirs) {
            if (Test-Path $iconSourceDir) {
              Copy-Item (Join-Path $iconSourceDir '*.svg') $iconTargetDir -Force
            }
          }

          $icon256SourceDir = Join-Path $workspace 'crates\keycord-lifecycle\data\branding\256x256\apps'
          $icon256TargetDir = Join-Path $shareDir 'icons\hicolor\256x256\apps'
          if (Test-Path $icon256SourceDir) {
            New-Item -ItemType Directory -Path $icon256TargetDir -Force | Out-Null
            Copy-Item (Join-Path $icon256SourceDir '*.png') $icon256TargetDir -Force
          }

          $glibSchemasDir = Join-Path $shareDir 'glib-2.0\schemas'
          New-Item -ItemType Directory -Path $glibSchemasDir -Force | Out-Null
          $schemaFile = Join-Path $mingwRoot 'share\glib-2.0\schemas\gschemas.compiled'
          Copy-Item $schemaFile $glibSchemasDir -Force

          $fontConfigDir = Join-Path $mingwRoot 'share\fontconfig'
          if (Test-Path $fontConfigDir) {
            Copy-Item $fontConfigDir (Join-Path $shareDir 'fontconfig') -Recurse -Force
          }

          $etcDir = Join-Path $mingwRoot 'etc'
          if (Test-Path $etcDir) {
            Copy-Item $etcDir (Join-Path $bundleDir 'etc') -Recurse -Force
          }

          $pixbufRoot = Join-Path $bundleDir 'lib\gdk-pixbuf-2.0\2.10.0'
          $pixbufLoadersDir = Join-Path $pixbufRoot 'loaders'
          New-Item -ItemType Directory -Path $pixbufLoadersDir -Force | Out-Null

          $pixbufSourceDir = Join-Path $mingwRoot 'lib\gdk-pixbuf-2.0\2.10.0\loaders'
          if (Test-Path $pixbufSourceDir) {
            Copy-Item (Join-Path $pixbufSourceDir '*') $pixbufLoadersDir -Recurse -Force
          }

          $pixbufQuery = Join-Path $mingwBin 'gdk-pixbuf-query-loaders.exe'
          $pixbufLoaderDlls = @(Get-ChildItem $pixbufLoadersDir -Filter '*.dll' | Select-Object -ExpandProperty FullName)
          if ((Test-Path $pixbufQuery) -and $pixbufLoaderDlls.Count -gt 0) {
            $pixbufCache = & $pixbufQuery $pixbufLoaderDlls
            [System.IO.File]::WriteAllText((Join-Path $pixbufRoot 'loaders.cache'), ($pixbufCache -join [Environment]::NewLine))
          }

          Write-Host "Bundle contents:"
          Get-ChildItem $bundleDir -Recurse

          $zip = Join-Path $workspace 'keycord-windows.zip'
          if (Test-Path $zip) {
            Remove-Item $zip -Force
          }

          $src = Join-Path $bundleDir '*'
          Compress-Archive -Path $src -DestinationPath $zip -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: keycord-windows
          path: keycord-windows.zip

  bundle:
    runs-on: windows-latest
    needs:
      - build
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true
          fetch-depth: 0

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: keycord-windows
          path: dist

      - name: Extract artifact
        shell: pwsh
        run: |
          $zip = Join-Path $env:GITHUB_WORKSPACE 'dist\keycord-windows.zip'
          if (-not (Test-Path $zip)) { throw "ZIP not found: $zip" }

          if (Test-Path bundle) { Remove-Item bundle -Recurse -Force }
          New-Item -ItemType Directory -Force -Path bundle | Out-Null
          Expand-Archive -Path $zip -DestinationPath bundle -Force

          Write-Host "Extracted files:"
          Get-ChildItem bundle | Sort-Object Name | Format-Table Name, Length

      - name: Install filter tool
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-ntldd-git

      - name: Filter
        shell: pwsh
        run: |
          $exe = Join-Path $env:GITHUB_WORKSPACE 'bundle\keycord.exe'
          if (-not (Test-Path $exe)) { throw "EXE not found in bundle: $exe" }

          $ntldd = 'C:\msys64\mingw64\bin\ntldd.exe'
          if (-not (Test-Path $ntldd)) {
            Write-Host "ntldd not present on this runner; skipping filtering."
            exit 0
          }

          function Get-NeededDllNames([string]$binaryPath) {
            $raw = & $ntldd -R $binaryPath 2>$null
            $raw |
              ForEach-Object { $_.Trim() } |
              Where-Object { $_ -match '^[A-Za-z]:\\' -and $_.ToLower().EndsWith('.dll') } |
              ForEach-Object { Split-Path $_ -Leaf }
          }

          $needed = New-Object 'System.Collections.Generic.HashSet[string]' ([System.StringComparer]::OrdinalIgnoreCase)
          foreach ($name in (Get-NeededDllNames $exe)) {
            $null = $needed.Add($name)
          }

          $loaderDir = Join-Path $env:GITHUB_WORKSPACE 'bundle\lib\gdk-pixbuf-2.0\2.10.0\loaders'
          if (Test-Path $loaderDir) {
            Get-ChildItem $loaderDir -Filter '*.dll' | ForEach-Object {
              foreach ($name in (Get-NeededDllNames $_.FullName)) {
                $null = $needed.Add($name)
              }
            }
          }

          $bundleDlls = Get-ChildItem bundle -Filter '*.dll' | Select-Object -ExpandProperty Name
          $remove = $bundleDlls | Where-Object { -not $needed.Contains($_) }

          # Keep it conservative: only remove if ntldd says it's not needed
          foreach ($name in $remove) {
            Remove-Item (Join-Path bundle $name) -Force
          }

          Write-Host "After filtering:"
          Get-ChildItem bundle | Sort-Object Name | Format-Table Name, Length

      - name: Install WiX Toolset
        run: choco install wixtoolset --no-progress -y

      - name: Bundle
        shell: pwsh
        run: |
          $heat = (Get-Command heat.exe -ErrorAction Stop).Source
          Write-Host "Using heat: $heat"

          New-Item -ItemType Directory -Force -Path wix\generated | Out-Null
          & $heat dir "bundle" `
            -cg AppFiles `
            -dr INSTALLDIR `
            -gg -g1 -srd -sreg -sfrag -suid `
            -var var.BundleDir `
            -out "wix\generated\AppFiles.wxs"

      - name: Get latest tag
        run: |
          $latest_tag = git describe --tags $(git rev-list --tags --max-count=1)
          echo "Latest tag: $latest_tag"
          "LATEST_TAG=$latest_tag" >> $env:GITHUB_ENV

      - name: Build
        shell: pwsh
        run: |
          $candle = (Get-Command candle.exe -ErrorAction Stop).Source
          $light  = (Get-Command light.exe  -ErrorAction Stop).Source

          Write-Host "Using candle: $candle"
          Write-Host "Using light : $light"

          $ver = "${{ env.LATEST_TAG }}" -replace '^v',''

          $parts = $ver.Split('.')
          if ($parts.Count -eq 3) { $ver = "$ver.0" }
          elseif ($parts.Count -eq 2) { $ver = "$ver.0.0" }
          elseif ($parts.Count -eq 1) { $ver = "$ver.0.0.0" }

          Write-Host "Using ProductVersion: $ver"

          & $candle `
            -ext WixUtilExtension `
            -dBundleDir="$(Resolve-Path bundle)" `
            -dProductVersion="$ver" `
            -out wix\generated\ `
            crates\keycord-lifecycle\data\branding\keycord.wxs `
            wix\generated\AppFiles.wxs

          & $light `
            -ext WixUtilExtension `
            -out keycord.msi `
            wix\generated\keycord.wixobj `
            wix\generated\AppFiles.wixobj

      - name: Upload MSI artifact
        uses: actions/upload-artifact@v4
        with:
          name: keycord-windows-msi
          path: keycord.msi

  publish:
    runs-on: windows-latest
    needs: bundle
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true
          fetch-depth: 0

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: keycord-windows-msi
          path: dist

      - name: Get latest tag
        run: |
          $latest_tag = git describe --tags $(git rev-list --tags --max-count=1)
          echo "Latest tag: $latest_tag"
          "LATEST_TAG=$latest_tag" >> $env:GITHUB_ENV

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.LATEST_TAG }}
          files: dist/keycord.msi
