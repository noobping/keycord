<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="io.github.noobping.keycord" path="/io/github/noobping/keycord/">
    <key name="password-store-dirs" type="as">
      <summary>Password store paths</summary>
      <description>
        List of password store directories.
      </description>
      <default>[]</default>
    </key>

    <key name="window-width" type="i">
      <default>850</default>
      <summary>Main window width</summary>
      <description>
        Last saved main window width.
      </description>
    </key>

    <key name="window-height" type="i">
      <default>600</default>
      <summary>Main window height</summary>
      <description>
        Last saved main window height.
      </description>
    </key>

    <key name="pass-command" type="s">
      <default>'pass'</default>
      <summary>Host</summary>
      <description>
        Command used for the Host backend.
      </description>
    </key>

    <key name="backend" type="s">
      <default>'integrated'</default>
      <summary>Password backend</summary>
      <description>
        Backend used to open the password store. Valid values are "integrated" and "host".
      </description>
    </key>

    <key name="new-pass-file-template" type="s">
      <default>'username:\nemail:\nurl:'</default>
      <summary>New pass file template</summary>
      <description>
        Lines added after the password line when creating a new password entry.
      </description>
    </key>

    <key name="clear-empty-fields-before-save" type="b">
      <default>false</default>
      <summary>Clear empty fields before save</summary>
      <description>
        Automatically remove blank structured fields from pass files before validation and save.
      </description>
    </key>

    <key name="username-fallback-mode" type="s">
      <default>'filename'</default>
      <summary>Username fallback mode</summary>
      <description>
        Username fallback used when a pass file has no encrypted username field. Valid values are "folder" and "filename".
      </description>
    </key>

    <key name="password-list-sort-mode" type="s">
      <default>'hybrid'</default>
      <summary>Password list sort mode</summary>
      <description>
        Password list ordering. Valid values are "store-path", "filename", and "hybrid".
      </description>
    </key>

    <key name="password-generator-length" type="u">
      <default>24</default>
      <summary>Password generator length</summary>
      <description>
        Total length for generated passwords.
      </description>
    </key>

    <key name="password-generator-min-lowercase" type="u">
      <default>1</default>
      <summary>Minimum lowercase letters</summary>
      <description>
        Minimum number of lowercase letters in generated passwords. Use 0 to disable lowercase letters.
      </description>
    </key>

    <key name="password-generator-min-uppercase" type="u">
      <default>1</default>
      <summary>Minimum uppercase letters</summary>
      <description>
        Minimum number of uppercase letters in generated passwords. Use 0 to disable uppercase letters.
      </description>
    </key>

    <key name="password-generator-min-numbers" type="u">
      <default>1</default>
      <summary>Minimum numbers</summary>
      <description>
        Minimum number of digits in generated passwords. Use 0 to disable numbers.
      </description>
    </key>

    <key name="password-generator-min-symbols" type="u">
      <default>1</default>
      <summary>Minimum symbols</summary>
      <description>
        Minimum number of symbols in generated passwords. Use 0 to disable symbols.
      </description>
    </key>

    <key name="ripasso-own-fingerprint" type="s">
      <default>''</default>
      <summary>Selected private key</summary>
      <description>
        Fingerprint of the private key used by the integrated backend.
      </description>
    </key>

    <key name="sync-private-keys-with-host" type="b">
      <default>false</default>
      <summary>Sync private keys with the host</summary>
      <description>
        Keep app-managed private keys aligned with the host GPG keyring when host access is available.
      </description>
    </key>

    <key name="audit-use-commit-history-recipients" type="b">
      <default>false</default>
      <summary>Use commit-history recipients for audit verification</summary>
      <description>
        When enabled, the audit tool falls back to recipient files from a commit's tree if the current branch recipients cannot authorize the signer.
      </description>
    </key>

    <key name="filter-included-store-roots" type="as">
      <default>[]</default>
      <summary>Stores included by shared filters</summary>
      <description>
        Store roots selected in shared store filters, including the password list and audit tool.
      </description>
    </key>

    <key name="audit-filter-included-branches" type="as">
      <default>[]</default>
      <summary>Branches included by the audit filter</summary>
      <description>
        Git branch names selected in the audit tool filter.
      </description>
    </key>

    <key name="hidden-notices" type="as">
      <default>[]</default>
      <summary>Hidden notices</summary>
      <description>
        Notice identifiers that should stay hidden after being dismissed.
      </description>
    </key>
  </schema>
</schemalist>
